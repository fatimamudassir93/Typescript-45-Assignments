"use strict";
function make_shirt(size, print_message) {
    console.log(`You have selected ${size} size and this message "${print_message}" to print on your shirt`);
}
make_shirt("large", "Sky is the limit...");
